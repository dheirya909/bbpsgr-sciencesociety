:root {
    --dark-bg: #ffffff;
    --dark-secondary: #f5f5f5;
    --dark-tertiary: #e8e8e8;
    --dark-text: #1a1a1a;
    --accent-color: #1a1a1a;
    --accent-secondary: #666666;
    --accent-tertiary: #333333;
    --hover-color: #000000;
    --glow-color: #000000;
}

* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    scroll-behavior: smooth;
}

body {
    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
    background-color: var(--dark-bg);
    color: var(--dark-text);
    line-height: 1.6;
    overflow-x: hidden;
}

/* ====== MINIMAL HERO SECTION ====== */
.hero {
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 0 5%;
    padding-top: 80px;
    position: relative;
    background: var(--dark-bg);
    overflow: hidden;
}

.logo-background {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 800px;
    height: 800px;
    opacity: 0.08;
    pointer-events: none;
    z-index: 0;
}

.logo-background img {
    width: 100%;
    height: 100%;
    object-fit: contain;
}

.hero-content {
    text-align: center;
    position: relative;
    z-index: 1;
    max-width: 800px;
}

.hero h1 {
    font-size: clamp(2.8rem, 10vw, 5.5rem);
    font-weight: 300;
    letter-spacing: -0.03em;
    line-height: 1.1;
    margin-bottom: 1.5rem;
    color: var(--accent-color);
}

.hero p {
    font-size: clamp(1.2rem, 4vw, 1.8rem);
    font-weight: 300;
    letter-spacing: 0.05em;
    color: var(--accent-secondary);
    margin-bottom: 3rem;
    text-transform: uppercase;
}

.hero-nav {
    display: flex;
    gap: 2rem;
    justify-content: center;
    flex-wrap: wrap;
}

.hero-nav a {
    font-size: 1rem;
    font-weight: 500;
    letter-spacing: 0.1em;
    color: var(--accent-color);
    text-decoration: none;
    text-transform: uppercase;
    padding: 0.5rem 0;
    border-bottom: 2px solid transparent;
    transition: all 0.3s ease;
    position: relative;
}

.hero-nav a::after {
    content: '';
    position: absolute;
    bottom: -2px;
    left: 0;
    width: 0;
    height: 2px;
    background: var(--accent-color);
    transition: width 0.3s ease;
}

.hero-nav a:hover::after {
    width: 100%;
}

/* ====== RESPONSIVE HERO ====== */

.science-card::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: linear-gradient(45deg, transparent, rgba(0, 255, 212, 0.1));
    transform: translateX(-100%);
    transition: transform 0.6s ease;
}

.science-card::after {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 3px;
    background: linear-gradient(90deg, transparent, var(--accent-color), transparent);
    opacity: 0;
    transition: opacity 0.3s ease;
}

.science-card:hover {
    transform: translateY(-12px);
    border-color: var(--accent-color);
    box-shadow: 0 15px 40px rgba(0, 255, 212, 0.15), inset 0 0 20px rgba(0, 255, 212, 0.05);
}

.science-card:hover::before {
    transform: translateX(0);
}

.science-card:hover::after {
    opacity: 1;
}

.science-card i {
    font-size: 3rem;
    color: var(--accent-color);
    margin-bottom: 1.5rem;
    transition: all 0.3s ease;
}

.science-card:hover i {
    transform: scale(1.25) rotateZ(10deg);
    filter: drop-shadow(0 0 15px rgba(0, 255, 212, 0.5));
}

.science-card h3 {
    color: var(--accent-color);
    margin-bottom: 1rem;
    font-size: 1.5rem;
    font-weight: 600;
}

.science-card p {
    color: var(--dark-text);
    opacity: 0.85;
    margin-bottom: 1.5rem;
    flex-grow: 1;
}

.card-link {
    color: var(--accent-tertiary);
    text-decoration: none;
    font-weight: 600;
    transition: all 0.3s ease;
    display: inline-block;
}

.card-link:hover {
    color: var(--accent-color);
    transform: translateX(5px);
}


/* ====== STATS SECTION ====== */
.stats-section {
    padding: 6rem 5%;
    background: linear-gradient(to bottom, rgba(26, 31, 58, 0.9), rgba(44, 62, 95, 0.8));
    position: relative;
    overflow: hidden;
}

.stats-section::before {
    content: '';
    position: absolute;
    top: 30%;
    right: -20%;
    width: 500px;
    height: 500px;
    background: radial-gradient(circle, rgba(74, 144, 255, 0.1) 0%, transparent 70%);
    border-radius: 50%;
    filter: blur(40px);
    pointer-events: none;
}

.stats-section > h2 {
    text-align: center;
    font-size: 2.5rem;
    margin-bottom: 4rem;
    background: linear-gradient(135deg, var(--accent-color), var(--accent-tertiary));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
}

.stats-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: 2.5rem;
    max-width: 1200px;
    margin: 0 auto;
    position: relative;
    z-index: 1;
}

.stat-card {
    background: linear-gradient(135deg, rgba(26, 31, 58, 0.9), rgba(44, 62, 95, 0.9));
    border: 1px solid rgba(0, 255, 212, 0.2);
    border-radius: 12px;
    padding: 2.5rem 2rem;
    text-align: center;
    transition: all 0.3s ease;
    position: relative;
    overflow: hidden;
}
.science-card {
    display: none;
}

.science-grid {
    display: none;
}

.stats-section {
    display: none;
}

.stat-card {
    display: none;
}

.stat-number {
    display: none;
}

.stat-label {
    display: none;
}

.mission-section {
    display: none;
}

.mission-content {
    display: none;
}

.mission-text {
    display: none;
}

.mission-grid {
    display: none;
}

.mission-item {
    display: none;
}

.mission-icon {
    display: none;
}

.featured-projects {
    display: none;
}

.projects-grid {
    display: none;
}

.project-card {
    display: none;
}

.project-icon {
    display: none;
}

.project-tags {
    display: none;
}

.tag {
    display: none;
}

.cta-section {
    display: none;
}

.cta-buttons {
    display: none;
}

.cta-large {
    display: none;
}

/* ====== RESPONSIVE DESIGN ====== */
@media screen and (max-width: 768px) {
    .hero {
        min-height: calc(100vh - 70px);
    }

    .hero h1 {
        font-size: 2.5rem;
    }

    .hero p {
        font-size: 1.1rem;
    }

    .hero-nav {
        gap: 1.5rem;
    }

    .logo-background {
        width: 600px;
        height: 600px;
        opacity: 0.06;
    }
}

@media (max-width: 480px) {
    .hero {
        min-height: calc(100vh - 60px);
        padding: 0 3%;
    }

    .hero h1 {
        font-size: 1.8rem;
        margin-bottom: 1rem;
    }

    .hero p {
        font-size: 0.95rem;
        margin-bottom: 2rem;
    }

    .hero-nav {
        flex-direction: column;
        gap: 1rem;
        align-items: center;
    }

    .hero-nav a {
        display: block;
        width: 100%;
        text-align: center;
    }

    .logo-background {
        width: 400px;
        height: 400px;
        opacity: 0.05;
    }
}

@media (prefers-reduced-motion: reduce) {
    * {
        animation: none !important;
        transition: none !important;
    }
}
